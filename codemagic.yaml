workflows:
  flutter-ios-build:
    name: Sawa iOS Build
    instance_type: mac_mini_m1
    environment:
      flutter: stable
      xcode: latest
    scripts:
      - name: Flutter pub get
        script: flutter packages pub get
      - name: Fix iOS Deployment Target for SPM
        script: sed -i '' 's/IPHONEOS_DEPLOYMENT_TARGET = 13.0;/IPHONEOS_DEPLOYMENT_TARGET = 15.0;/g' ios/Runner.xcodeproj/project.pbxproj
      - name: Inject Dummy Development Team & Bypass Signing
        script: |
          # حقن الـ Team الوهمي وإعدادات التخطّي داخل ملف الـ الـ xcconfig الخاص بالـ Release
          echo "DEVELOPMENT_TEAM = 1A2B3C4D5E" >> ios/Runner/Release.xcconfig
          echo "CODE_SIGNING_ALLOWED = NO" >> ios/Runner/Release.xcconfig
          echo "CODE_SIGNING_REQUIRED = NO" >> ios/Runner/Release.xcconfig
          
          # تعديل ملف المشروع الرئيسي للتأكيد على الـ Team الوهمي ومنع توقف الـ SPM
          python3 -c "
          with open('ios/Runner.xcodeproj/project.pbxproj', 'r') as f:
              content = f.read()
          patched = content.replace('buildSettings = {', 'buildSettings = {\n\t\t\t\tDEVELOPMENT_TEAM = 1A2B3C4D5E;')
          with open('ios/Runner.xcodeproj/project.pbxproj', 'w') as f:
              f.write(patched)
          "
      - name: Flutter build iOS (Unsigned)
        script: flutter build ios --release --no-codesign
      - name: Package Unsigned IPA
        script: |
          mkdir -p build/ios/ipa/Payload
          cp -r build/ios/iphoneos/Runner.app build/ios/ipa/Payload/
          cd build/ios/ipa
          zip -r Sawa_Test.ipa Payload
    artifacts:
      - build/ios/ipa/*.ipa